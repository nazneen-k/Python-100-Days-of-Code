#A function is defined using the def keyword, followed by the function name, parentheses (which can include parameters), and a colon. The body of the function is indented.

# Parameters and Arguments
# Parameters: Variables listed inside the parentheses in the function definition.
# Arguments: Values passed to the function when it is called.

def my_function():
    print("Hello")
    print("Bye")

my_function()